const canvas=document.getElementById('starfield');if(canvas){const c=canvas.getContext('2d');let w,h;let stars=[];function R(n){return Math.random()*n}function resize(){w=canvas.width=innerWidth;h=canvas.height=innerHeight;stars=[...Array(220)].map(()=>({x:R(w),y:R(h),z:Math.random()*1+0.2,s:Math.random()*1.5+0.2}))}function tick(){c.clearRect(0,0,w,h);c.fillStyle='#9ab6ff';for(const st of stars){c.globalAlpha=st.z;c.fillRect(st.x,st.y,st.s,st.s);st.y+=st.z*0.3;if(st.y>h){st.y=0;st.x=R(w)}}requestAnimationFrame(tick)}addEventListener('resize',resize);resize();tick();}