let startTime=null,timerId=null;function startMeditation(){const a=document.getElementById('cosmicAudio');if(a.paused)a.play();startTime=Date.now();timerId=setInterval(updateTimer,250);document.getElementById('startBtn').disabled=true;document.getElementById('endBtn').disabled=false;}function endMeditation(){if(timerId){clearInterval(timerId);timerId=null;}const dur=Date.now()-startTime;const msg=formatDur(dur);localStorage.setItem('ps_best',String(Math.max(dur,Number(localStorage.getItem('ps_best')||0))));document.getElementById('result').innerText=`You held silence for ${msg}.`;}function formatDur(ms){const s=Math.floor(ms/1000);const m=Math.floor(s/60);const r=s%60;return `${m}m ${r}s`;}function updateTimer(){const dur=Date.now()-startTime;document.getElementById('live').innerText=formatDur(dur);}