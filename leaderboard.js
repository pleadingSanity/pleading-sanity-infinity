const canvas=document.getElementById('starsBoard');if(canvas){const ctx=canvas.getContext('2d');let w,h;function R(n){return Math.random()*n}function resize(){w=canvas.width=innerWidth;h=canvas.height=innerHeight-120}resize();addEventListener('resize',resize);const demo=[{name:'Luna',ms:725000,quote:'Silence is the language of the stars.'},{name:'Orion',ms:522000,quote:'Every second of stillness is another galaxy lit.'},{name:'Nova',ms:378000,quote:'The universe doesn\'t shout, it hums.'},{name:'Zenith',ms:296000,quote:'Calm is the highest form of rebellion.'},{name:'Echo',ms:213000,quote:'Even silence has rhythm.'}];const stars=demo.map(d=>({x:R(w),y:R(h),r:2+Math.min(10,d.ms/600000),data:d}));function draw(){ctx.clearRect(0,0,w,h);for(const s of stars){ctx.globalAlpha=.8;ctx.fillStyle='#9ab6ff';ctx.beginPath();ctx.arc(s.x,s.y,s.r,0,Math.PI*2);ctx.fill();}requestAnimationFrame(draw)}draw();canvas.addEventListener('click',(e)=>{const rect=canvas.getBoundingClientRect();const x=e.clientX-rect.left;const y=e.clientY-rect.top;for(const s of stars){const dx=x-s.x,dy=y-s.y;if(Math.hypot(dx,dy)<s.r+4){alert(`${s.data.name} — ${Math.floor(s.data.ms/60000)}m ${Math.floor((s.data.ms/1000)%60)}s\n${s.data.quote}`);break;}}});}